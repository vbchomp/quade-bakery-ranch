/* console.log("Hello, world"); // check console log in developer tools on browser



/* Cannot stop getting the NaN error. Not sure why it's not adding the two inputs. */
var length;
var width;
var area = lengthResponse * widthResponse;
var lengthPrompt = "Enter length";
var widthPrompt = "Enter width";
var lengthResponse = prompt(lengthPrompt);
var widthResponse = prompt(widthPrompt);
console.log(area);

var alertMessage = "The area is " + area + "!";

alert(alertMessage);

